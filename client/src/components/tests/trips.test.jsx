// import { render, screen } from "@testing-library/react";
// import App from "./App";

// const mockTrip = {
//   name: "Trip to Paris",
//   user_name: "Bon"
// };

// it("renders trips after user is logged in", () => {
//   render(<App trip={mockTrip} />);
//   const trips = screen.getByText("Trip to Paris");
//   expect(trips).toBeInTheDocument();
// });
